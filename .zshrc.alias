alias la='ls -a'
alias ll='ls -l'
alias lla='ls -la'
 
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
 
alias mkdir='mkdir -p'

# sudo の後のコマンドでエイリアスを有効にする
alias sudo='sudo '

# cd で移動先をサジェスト
alias pcd="powered_cd"

# tracking
alias tracking="~/go/src/github.com/netooo/tracking/tracking"
